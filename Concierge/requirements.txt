discord.py>=2.4,<3
aiohttp>=3.14.3,<4               # direct dep: finsearch_client uses aiohttp.ClientSession itself,
                             # not only transitively via discord.py — declare it explicitly
# dev/test
pytest>=8
pytest-asyncio>=1.4.0,<1.5   # auto mode (see pytest.ini); tests also carry explicit @pytest.mark.asyncio
