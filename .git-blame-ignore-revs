# Commits to skip in `git blame` because they only touch formatting / line
# endings, never real content. GitHub honors this file automatically. Locally,
# enable it once with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# chore: normalize line endings to LF via .gitattributes (EOL-only)
b836187582804fa0e87d79cffd9c19163816d1ef
